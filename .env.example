# Google Calendar credentials and tokens
# (handled by MCP - no keys needed here)

# Fireflies.ai
# (handled by MCP - no keys needed here)

# ClickUp API
CLICKUP_API_TOKEN=pk_your_token_here
CLICKUP_WORKSPACE_ID=your_workspace_id

# Add other API keys as needed
